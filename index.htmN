<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sondage Marketing - McDo Chocolat</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@500;700&display=swap" rel="stylesheet">
    
    <style>
        /*
        ==================================================
        CSS (STYLE MCDO THÉMATIQUE)
        ==================================================
        */
        :root {
            --sapin-green: #004d40; /* Vert Sapin / Forêt Profond */
            --light-cream: #f9f9f9; /* Crème très clair pour le fond des cartes */
            --accent-gold: #ffc72c; /* Jaune/Or McDo */
            --accent-red: #c00; /* Rouge McDo */
            --text-on-dark: #f0f0f0; /* Texte très clair pour le fond vert */
            --text-dark: #333; /* Texte sombre pour les cartes claires */
            --text-light: #555; /* Texte un peu plus clair pour les cartes claires */
            --mcdo-font: 'Oswald', sans-serif; /* Police style McDo */
            --body-font: Arial, sans-serif; /* Police de corps classique */
        }

        body {
            font-family: var(--body-font);
            background-color: var(--sapin-green); /* Fond Vert Sapin */
            color: var(--text-on-dark);
            margin: 0;
            padding: 30px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            background-color: var(--light-cream);
            padding: 50px;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
            max-width: 750px;
            width: 100%;
            border-top: 8px solid var(--accent-gold); 
            color: var(--text-dark); /* Le texte dans le container redevient sombre */
        }

        header {
            text-align: center;
            margin-bottom: 40px;
        }

        header h1 {
            font-family: var(--mcdo-font); /* TITRE en police McDo */
            color: var(--accent-gold); /* Titre principal en Jaune McDo */
            font-size: 3.2em; /* Titre plus grand */
            font-weight: 700;
            margin-bottom: 10px;
            line-height: 1.1;
        }

        header p {
            color: var(--text-light); 
            font-size: 1.2em;
        }

        /* Style des groupes de questions (cartes) */
        .question-group {
            margin-bottom: 30px;
            padding: 25px;
            border: none;
            border-radius: 10px;
            background-color: var(--bg-white); 
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08); 
        }

        .question-group h3 {
            font-family: var(--mcdo-font); /* SOUS-TITRES en police McDo */
            margin-top: 0;
            color: var(--sapin-green);
            font-size: 1.6em; /* Sous-titre plus grand et audacieux */
            font-weight: 500;
            border-left: 4px solid var(--accent-gold);
            padding-left: 10px;
            margin-bottom: 20px;
        }

        /* Styles des options (pour les rendre plus cliquables) */
        label {
            display: flex;
            align-items: center;
            margin: 15px 0;
            padding: 12px;
            border-radius: 8px;
            transition: background-color 0.2s, box-shadow 0.2s;
            cursor: pointer;
            border: 1px solid #eee;
        }

        label:hover {
            background-color: #fff3cc; /* Jaune clair au survol */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        /* Styles des boutons radio modernes */
        input[type="radio"] {
            appearance: none;
            width: 18px;
            height: 18px;
            border: 2px solid var(--accent-gold);
            border-radius: 50%;
            margin-right: 15px;
            display: grid;
            place-content: center;
            cursor: pointer;
            transition: border-color 0.2s;
        }

        input[type="radio"]::before {
            content: "";
            width: 10px;
            height: 10px;
            border-radius: 50%;
            transform: scale(0);
            transition: 120ms transform ease-in-out;
            box-shadow: inset 1em 1em var(--accent-red);
        }

        input[type="radio"]:checked {
            border-color: var(--accent-red);
        }
        
        input[type="radio"]:checked::before {
            transform: scale(1);
        }
        
        /* Styles des champs de texte/nombre */
        input[type="number"], input[type="text"] {
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 6px;
            width: 90%;
            margin-top: 10px;
            font-size: 1em;
            transition: border-color 0.2s;
        }
        
        input[type="number"]:focus, input[type="text"]:focus {
            border-color: var(--accent-gold);
            outline: none;
        }


        /* Style du Range Slider */
        input[type="range"] {
            width: 100%;
            margin-top: 15px;
        }
        .range-labels {
            display: flex;
            justify-content: space-between;
            margin-top: 5px;
            font-size: 0.9em;
            color: var(--text-light);
            font-weight: normal;
        }

        /* Bouton de soumission */
        button {
            font-family: var(--mcdo-font); /* Bouton en police McDo */
            display: block;
            width: 100%;
            padding: 18px;
            background-color: var(--accent-red);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 1.5em; /* Bouton plus grand */
            font-weight: 700;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.2s;
            margin-top: 30px;
            letter-spacing: 2px;
        }

        button:hover {
            background-color: #a00;
            transform: translateY(-3px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
        }

        /* Style pour les résultats (Post-soumission) */
        #results {
            margin-top: 40px;
            padding: 30px;
            border: 2px solid var(--sapin-green);
            background-color: #e8f5e9;
            border-radius: 10px;
            display: none;
            text-align: left;
            color: var(--text-dark);
        }

        #results h2 {
            font-family: var(--mcdo-font);
            color: var(--sapin-green);
            text-align: center;
        }
        
        #results p {
            font-size: 1.1em;
            margin: 10px 0;
            padding-left: 10px;
            border-left: 3px solid var(--accent-gold);
        }
        
        .note {
            font-style: italic;
            font-size: 0.8em;
            color: #999;
            text-align: center;
            margin-top: 20px;
        }
    </style>
</head>
<body>

<div class="container">
    <header>
        <h1>Sondage Marketing : Le Chocolat McDo</h1>
        <p>Aidez-nous à choisir la meilleure stratégie pour le lancement de notre tablette de chocolat.</p>
    </header>

    <form id="marketingSurvey">
        
        <div class="question-group">
            <h3>1. Quel concept vous attire le plus ?</h3>
            <label>
                <input type="radio" name="concept_preference" value="McDo_Wonka" required>
                Tablette de chocolat en **partenariat McDo X Willy Wonka** (Édition Limitée, Magie, Rêve)
            </label>
            <label>
                <input type="radio" name="concept_preference" value="McDo_Seul">
                Tablette de chocolat de la **marque McDo seule** (Goût iconique, Familial, Classique)
            </label>
        </div>

        <div class="question-group">
            <h3>2. Si c'était la tablette McDo X Willy Wonka, quelle est votre intention d'achat ? (1 = Pas du tout, 5 = Certainement)</h3>
            <input type="range" id="intent_wonka" name="intent_wonka" min="1" max="5" value="3" oninput="document.getElementById('value_wonka').innerText=this.value">
            <div class="range-labels">
                <span>1</span>
                <span>2</span>
                <span>3</span>
                <span>4</span>
                <span>5</span>
            </div>
            <p>Score sélectionné : <strong id="value_wonka">3</strong></p>
        </div>

        <div class="question-group">
            <h3>3. Si c'était la tablette McDo seule, quelle est votre intention d'achat ? (1 = Pas du tout, 5 = Certainement)</h3>
            <input type="range" id="intent_seul" name="intent_seul" min="1" max="5" value="3" oninput="document.getElementById('value_seul').innerText=this.value">
            <div class="range-labels">
                <span>1</span>
                <span>2</span>
                <span>3</span>
                <span>4</span>
                <span>5</span>
            </div>
            <p>Score sélectionné : <strong id="value_seul">3</strong></p>
        </div>

        <div class="question-group">
            <h3>4. Quel prix vous semblerait juste pour une tablette de chocolat McDo de qualité standard ? (en €)</h3>
            <label>Prix idéal (€) : 
                <input type="number" name="price" placeholder="Ex: 2.50" step="0.01" min="1" required>
            </label>
        </div>

        <button type="submit">Valider mes réponses</button>
    </form>

    <div id="results">
        <h2>✅ Vos Réponses (Simulation)</h2>
        <p><strong>Concept Préféré :</strong> <span id="res_concept"></span></p>
        <p><strong>Intention Wonka :</strong> <span id="res_wonka"></span> / 5</p>
        <p><strong>Intention Seule :</strong> <span id="res_seul"></span> / 5</p>
        <p><strong>Prix Suggéré :</strong> <span id="res_price"></span> €</p>
        <p class="note">**NOTE IMPORTANTE :** Dans une campagne réelle, ces données seraient envoyées à une base de données (via un service backend) et non affichées ici.</p>
    </div>

</div>

<script>
    /*
    ==================================================
    JAVASCRIPT (LOGIQUE)
    ==================================================
    */
    document.getElementById('marketingSurvey').addEventListener('submit', function(event) {
        event.preventDefault();

        const form = event.target;
        const concept = form.querySelector('input[name="concept_preference"]:checked');
        const intentWonka = form.elements['intent_wonka'].value;
        const intentSeul = form.elements['intent_seul'].value;
        const price = form.elements['price'].value;

        if (!concept) {
            alert("Veuillez choisir un concept préféré pour continuer.");
            return;
        }
        
        const data = {
            concept: concept.value,
            intent_wonka: intentWonka,
            intent_seul: intentSeul,
            price: price
        };
        
        console.log("Données collectées (à envoyer) :", data);

        document.getElementById('res_concept').innerText = (concept.value === 'McDo_Wonka' ? 'McDo X Willy Wonka' : 'McDo Seul');
        document.getElementById('res_wonka').innerText = intentWonka;
        document.getElementById('res_seul').innerText = intentSeul;
        document.getElementById('res_price').innerText = price;
        
        document.getElementById('marketingSurvey').style.display = 'none'; 
        document.getElementById('results').style.display = 'block'; 
        
        alert("Merci pour votre participation !");
    });
</script>

</body>
</html>
